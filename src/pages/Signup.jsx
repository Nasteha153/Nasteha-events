import { useState } from 'react'
import { Link, useNavigate } from 'react-router'
import { FiArrowRight, FiLock, FiMail, FiUser } from 'react-icons/fi'
import { useAuth } from '../context/AuthContext'
export default function Signup(){const{signUp}=useAuth();const navigate=useNavigate();const[form,setForm]=useState({fullName:'',email:'',password:''});const[error,setError]=useState('');const[loading,setLoading]=useState(false);const[done,setDone]=useState(false);async function submit(e){e.preventDefault();setError('');setLoading(true);try{await signUp(form);setDone(true)}catch(err){setError(err.message||'Could not sign up.')}finally{setLoading(false)}}if(done)return <div className="mx-auto flex min-h-[70vh] max-w-lg items-center px-4 py-16"><div className="surface w-full rounded-[2rem] p-8 text-center"><div className="mx-auto flex h-16 w-16 items-center justify-center rounded-3xl bg-emerald-400/10 text-3xl">📬</div><h1 className="mt-5 font-display text-2xl font-bold text-white">Check your inbox</h1><p className="mt-3 text-sm leading-6 text-slate-500">We've sent a confirmation link to <strong className="text-slate-300">{form.email}</strong>. Confirm your email, then log in.</p><button onClick={()=>navigate('/login')} className="btn-primary mt-6 w-full">Go to login</button></div></div>;return <AuthLayout title="Create your account" text="Join Nasteha Events and start discovering or creating meaningful gatherings."><form onSubmit={submit} className="space-y-5">{error&&<p className="rounded-xl border border-rose-400/20 bg-rose-400/10 p-3 text-sm text-rose-300">{error}</p>}<Field icon={<FiUser/>} label="Full name"><input required className="input-dark pl-10" value={form.fullName} onChange={e=>setForm({...form,fullName:e.target.value})} placeholder="Nasteha Mohamed"/></Field><Field icon={<FiMail/>} label="Email"><input required type="email" className="input-dark pl-10" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="you@example.com"/></Field><Field icon={<FiLock/>} label="Password"><input required minLength={6} type="password" className="input-dark pl-10" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="At least 6 characters"/></Field><button disabled={loading} className="btn-primary w-full">{loading?'Creating account…':'Create account'} <FiArrowRight/></button></form><p className="mt-6 text-center text-sm text-slate-500">Already have an account? <Link to="/login" className="font-bold text-indigo-300 hover:text-white">Sign in</Link></p></AuthLayout>}
function AuthLayout({title,text,children}){return <div className="mx-auto grid min-h-[calc(100vh-150px)] max-w-7xl items-center gap-10 px-4 py-14 sm:px-6 lg:grid-cols-2"><div className="hidden lg:block"><p className="text-xs font-bold uppercase tracking-[.2em] text-cyan-300">Start here</p><h2 className="mt-4 max-w-xl font-display text-5xl font-extrabold leading-tight text-white">Your next event starts with one simple account.</h2><p className="mt-5 max-w-lg leading-7 text-slate-500">Create a profile, explore events, register for what interests you, or publish your own gathering.</p></div><div className="mx-auto w-full max-w-md"><div className="surface rounded-[2rem] p-6 sm:p-8"><p className="text-xs font-bold uppercase tracking-[.18em] text-indigo-300">Nasteha Events</p><h1 className="mt-2 font-display text-3xl font-bold text-white">{title}</h1><p className="mt-2 text-sm leading-6 text-slate-500">{text}</p><div className="mt-7">{children}</div></div></div></div>}
function Field({icon,label,children}){return <div><label className="mb-2 block text-sm font-semibold text-slate-200">{label}</label><div className="relative"><span className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-500">{icon}</span>{children}</div></div>}
